apiVersion: v1
kind: Secret
metadata:
  name: {{ include "jwt-module.fullname" . }}-secrets
  labels:
    {{- include "jwt-module.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
