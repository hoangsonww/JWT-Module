apiVersion: apps/v1
kind: Deployment
metadata:
  name: jwt-module
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: jwt-module
          resources:
            requests:
              cpu: 64m
              memory: 64Mi
            limits:
              cpu: 256m
              memory: 128Mi
