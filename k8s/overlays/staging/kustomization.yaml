apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: jwt-module

resources:
  - ../../base

patches:
  - path: patches/deployment-patch.yaml
  - path: configmap.yaml

commonLabels:
  environment: staging
